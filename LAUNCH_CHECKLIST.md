# SprinklerSmart — Launch Checklist

> **Status as of 2026-06-15** · Production live at [sprinkler-app-psi.vercel.app](https://sprinkler-app-psi.vercel.app) · CI green · 78 unit + 24 E2E tests passing

---

## Track 1 — Affiliate Revenue (Path 1 · ~100% margin · no backend)

- [ ] **Create Amazon Associates account** — `affiliate-program.amazon.com`, use `jeff.jones@desertservicesgroup.com` + DSG identity. Website field: `https://sprinkler-app-psi.vercel.app`.
- [ ] **Get your Associates tag** — looks like `desertservices-20`. Amazon reviews in 1–3 business days.
- [ ] **Give tag to Dispatch Agent 2** — agent will set `VITE_AFFILIATE_TAG` in Vercel and redeploy. That's it. Zero code changes.
- [ ] **Verify live** — visit the app, draw a zone, place heads, click any "Buy →" link. URL should contain `?tag=desertservices-20`.

**Economics:** ~300 plan-completers/mo × 8% buy-through × $200 cart × 3% = **~$144/mo at ~100% margin**.

---

## Track 2 — Pro Plan $19 (Path 2 · Stripe paywall)

### 2a · Stripe setup (Jeff, ~15 min)
- [ ] Log into `dashboard.stripe.com`
- [ ] **Create Product** — Name: `SprinklerSmart Pro Plan`, Price: **$19.00 USD, one-time**
- [ ] **Copy Price ID** — looks like `price_1AbcXYZ…`
- [ ] **Copy API keys** — Publishable (`pk_live_…`) and Secret (`sk_live_…`)
- [ ] **Give all three values to Dispatch Agent 2** — agent stores secret in vault + sets Vercel env vars

### 2b · Code gate (Dispatch Agent 1 — runs autonomously NOW)
- [ ] Agent 1 PR merged and CI green — Stripe gate code (localStorage persist → Checkout redirect → on return auto-download PDF)
- [ ] `?checkout=success` handler tested in staging

### 2c · Go-live gate
- [ ] `VITE_STRIPE_PK`, `STRIPE_SECRET_KEY`, `STRIPE_PRICE_ID` set in Vercel production
- [ ] Test end-to-end with Stripe test card `4242 4242 4242 4242`
- [ ] Switch to live keys → production redeploy

---

## Track 3 — Analytics (PostHog · free)

- [ ] Sign up at `posthog.com` — create project `SprinklerSmart`
- [ ] Copy **Project API key** — looks like `phc_abc123…`
- [ ] **Give key to Dispatch Agent 2** — agent sets `POSTHOG_API_KEY` in Vercel
- [ ] Verify events landing in PostHog: `session_start`, `plan_designed`, `pro_plan_initiated`, `affiliate_clicked`
- [ ] Watch `plan_designed → pro_plan_initiated` funnel to confirm PDF is worth $19 before gating hard

---

## Track 4 — iOS / TestFlight

- [ ] Enroll in **Apple Developer Program** ($99/yr) if not already — `developer.apple.com`
- [ ] Provision a **cloud Mac** (MacStadium, GitHub Actions macOS runner, or similar)
- [ ] On the Mac: `npm ci && npm run build && npx cap add ios && npx cap sync ios`
- [ ] Add location string to `ios/App/App/Info.plist` (see `docs/MOBILE_TESTFLIGHT.md`)
- [ ] Open in Xcode → set Team → Archive → Upload to App Store Connect
- [ ] Create TestFlight group → invite testers
- [ ] Submit build to Apple review

Full step-by-step in `docs/MOBILE_TESTFLIGHT.md`.

---

## Dispatch Agent Prompts

### Agent 1 — Run NOW (no Jeff input needed)

Wires the Stripe payment gate in code. See the guide for the full prompt.  
**Branch:** `feat/stripe-payment-gate` · **Blocks:** Track 2c

### Agent 2 — Run AFTER Jeff completes Tracks 1–3 signups

Sets all Vercel env vars using the provided values + triggers redeploy.  
Vercel CLI is already authenticated (`vercel whoami` → `triplej84-9345`).  
**Unblocks:** affiliate revenue (immediate), Pro Plan paywall, PostHog analytics.

---

## Current Code State

| Item | Status |
|---|---|
| Core planner (zones, heads, auto-place, savings) | ✅ Shipped |
| Affiliate links (Amazon, real URLs) | ✅ Live — needs tag in Vercel |
| PDF blueprint export (jsPDF, valve schedule) | ✅ Shipped — free until Stripe gate |
| Stripe serverless function (`api/checkout.ts`) | ✅ Shipped — needs env vars |
| Analytics event sink (`api/events.ts`) | ✅ Shipped — needs PostHog key |
| Stripe payment gate (localStorage → redirect → auto-download) | ⬜ Agent 1 |
| Vercel env vars (affiliate tag, Stripe, PostHog) | ⬜ Agent 2 |
| iOS / TestFlight | ⬜ Jeff on Mac |
| Head drag polish + touch targets | ⬜ Future |
| Second affiliate tag (Home Depot / SiteOne via Impact) | ⬜ Future |

---

## Revenue Activation Order

```
1. Agent 2 sets VITE_AFFILIATE_TAG   →  affiliate revenue ON  (today, after Associates signup)
2. Agent 2 sets Stripe env vars      →  $19 Pro Plan ON       (after Agent 1 PR + Stripe setup)
3. Agent 2 sets POSTHOG_API_KEY      →  funnel data ON        (after PostHog signup)
4. Jeff bootstraps iOS               →  TestFlight            (when you have Mac time)
```

---

*Generated by Claude Code · Desert Services Group LLC · SprinklerSmart*
